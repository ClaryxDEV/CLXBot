const Discord = require("discord.js");
const {PREFIX} = require("../config.js");

module.exports.run = (client, message, args) => {

}


module.exports.help = {
    name: '*le nom de ta commande*',
    aliases: ["*les aliases si il y en as pas tu met juste le nom de la cmd*"]
  };
